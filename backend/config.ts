const CONFIG = {
    /**
     * Absolute path to the directory where projects are stored.
     * 
     * Will be used to read/write project scene files.
     */
    PROJECTS_DIR_ABSOLUTE_PATH: "",
} as const;

export default CONFIG;